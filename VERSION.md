# 踏歌行智策通 - 版本号管理中心

> **单一数据源原则**：本文档是项目唯一版本号权威来源，所有文件版本号必须与此同步。
> 每次版本更新，先改此文件，再批量同步到其他文件。

---

## 当前版本

| 字段 | 值 | 说明 |
|------|-----|------|
| **主版本号** | V2.2.0 | 语义化版本，遵循 Keep a Changelog |
| **版本代号** | AVJ最终修复版 | 本版本核心目标 |
| **发布日期** | 2026-06-18 | 参赛截止日 |
| **Git分支** | dev-v2.3 | 开发分支 |
| **Git提交** | `6912790` | 最终提交哈希 |
| **提交时间** | 2026-06-19 15:11 | 最后推送时间 |

---

## 版本号同步清单（必须全部一致）

| 文件 | 路径 | 当前值 | 同步状态 |
|------|------|--------|----------|
| CHANGELOG.md | `/CHANGELOG.md` | [2.2.0] | ✅ 已同步 |
| VERSION.md | `/VERSION.md` | V2.2.0 | ✅ 本文档 |
| project_card.yaml | `/avj-submission/project_card.yaml` | V2.2.0 | ✅ 已同步 |
| project.config.json | `/project.config.json` | V2.2.0 | ✅ 已同步 |
| project.miniapp.json | `/project.miniapp.json` | 2.2.0 | ✅ 已同步 |

> 注：`submission.json` 已按 AVJ GitCode 标准删除（引擎直接读 YAML）；`app.json` 已移除 `version` 字段（微信小程序不支持自定义）。

---

## 版本历史（只记录有代码变更的版本）

### V2.2.0 (2026-06-18) - AVJ最终修复版
**目标**：提交参赛，修复所有AVJ材料和小程序Bug

#### 主要变更
- **AVJ材料**：11个YAML文件格式修复，AVJ引擎跑分50分（吃满E1上限）
- **小程序导航**：S1-S4 redirectTo不压栈，W1-W9 navigateTo逐页后退
- **Bug修复**：W8标题修正、s4-guide/s1-cover语法错误修复
- **新增文件**：OPENSOURCE.md、submission.json
- **版本统一**：所有文件版本号统一为V2.2.0

#### 文件变更统计
```
修改：11个YAML + 6个JS + 4个JSON + 2个MD
新增：OPENSOURCE.md, submission.json
```

---

### V2.1.0 (2026-06-11) - AVJ对齐版
**目标**：首次对齐AVJ框架，生成11个YAML参赛材料

#### 主要变更
- **新增**：avj-submission/目录（11个YAML）
- **新增**：utils/rules.json（18条结构化规则）
- **新增**：utils/trace.js（决策日志）
- **改造**：engine.js从硬编码改为加载rules.json

---

### V2.0.0 (2026-06-01) - 九步SOP完整版
**目标**：W1-W9全页面可运行，四维评分引擎可用

#### 主要变更
- **新增**：13个页面（S1-S4 + W1-W9）
- **新增**：四维评分引擎（合规×空间×经济×市场）
- **新增**：mock-data.js三层数据体系
- **新增**：Design System V1.0

---

### V1.0.0 (2026-05-15) - 原型版
**目标**：验证MVP可行性，基础评分逻辑

#### 主要变更
- 初始项目骨架
- 硬编码评分逻辑
- 静态演示数据

---

## 版本号规则

```
主版本号.次版本号.修订号
  │        │        │
  │        │        └── Bug修复、小优化（向后兼容）
  │        └── 新功能、大改造（向后兼容）
  └── 破坏性变更、架构重构（不兼容）
```

**当前阶段**：V2.x（功能完善期）
- V2.0 = 九步SOP完整版
- V2.1 = AVJ对齐版
- V2.2 = AVJ最终修复版（参赛版）

**未来规划**：
- V3.0 = 真实API接入版（腾讯地图/混元大模型）
- V4.0 = 商业化版本（用户系统、付费功能）

---

## 版本更新SOP（标准操作流程）

每次发版必须按以下顺序执行：

1. **更新VERSION.md**：修改"当前版本"和"版本历史"
2. **同步其他文件**：按"版本号同步清单"逐个修改
3. **更新CHANGELOG.md**：追加新版本条目
4. **Git提交**：commit信息包含版本号
5. **打标签**：`git tag v2.2.0`
6. **推送**：`git push github dev-v2.3 --tags`

---

> **检查命令**：
> ```bash
> grep -r "V2\.2\.0\|2\.2\.0\|v0\.0" project.config.json project.miniapp.json avj-submission/submission.json app.json
> ```
